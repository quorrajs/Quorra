
var config = {
    // @param {Function} for JSON.stringify
    'jsonReplacer': null,

    // @param {Number|String} for JSON.stringify
    'jsonSpaces': false,

    'jsonpCallbackName': 'callback'
};

module.exports = config;

